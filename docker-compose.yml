services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
    # Overriding command to enable reload for development
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
